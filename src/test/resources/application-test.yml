spring:
  application:
    name: fastfood-producao
  redis:
    host: redis
    port: 6379
    timeout: 60000
    jedis:
      pool:
        max-active: 10
        max-idle: 5
        min-idle: 1
  cache:
    type: redis